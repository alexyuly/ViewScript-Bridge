# ViewScript-Bridge

Source code for the ViewScript Bridge

```ts
import { view, element } from "viewscript-bridge";

view("Hello, world!", [
  element("p", {
    content: "Hello, world!",
  }),
]);
```
